from setuptools import setup, find_packages

setup(
    name="liv_spectral_framework",
    version="0.1.0",
    author="Alberto Rosales de Leon",
    description="Framework for spectral LIV studies using EBL absorption in blazar spectra",
    packages=find_packages(),
    python_requires=">=3.9.21",
)
